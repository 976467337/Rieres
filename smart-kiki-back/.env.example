# Database Configuration - Postgres local (docker-compose), separado do pantheonconcursos
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=smartkiki_db
DB_SSLMODE=disable

# Server Configuration
SERVER_PORT=8090
SERVER_ENV=development

# JWT Configuration
JWT_SECRET=change_me_in_production
JWT_EXPIRATION=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
